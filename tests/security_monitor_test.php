<?php
require_once __DIR__.'/../config/database.php';require_once __DIR__.'/../includes/security_monitor.php';
$db=(new Database())->getConnection();if(!$db)throw new RuntimeException('Database unavailable');
$_SERVER['REMOTE_ADDR']='198.51.100.77';$_SERVER['REQUEST_METHOD']='GET';$_SERVER['REQUEST_URI']='/security-test';$_SERVER['HTTP_USER_AGENT']='KitchenMartSecurityTest/1.0';
$db->beginTransaction();try{$score1=recordSecurityEvent($db,'test.anomaly',10,null,['test'=>true],'tested');$score2=recordSecurityEvent($db,'test.anomaly',10,null,['test'=>true],'tested');if($score1!==10||$score2!==20)throw new RuntimeException('Risk score accumulation failed');$hash=securityIpHash();securityBlock($db,'ip',$hash,null,'test block',60,60);securityBlock($db,'ip',$hash,null,'test block',60,60);$count=$db->prepare("SELECT COUNT(*) FROM security_blocks WHERE target_type='ip' AND target_hash=? AND is_active=1");$count->execute([$hash]);if((int)$count->fetchColumn()!==1)throw new RuntimeException('Repeated blocks must update the active row instead of creating duplicates');if(!securityIsBlocked($db))throw new RuntimeException('Temporary block check failed');$db->rollBack();echo "Security monitor tests passed\n";}catch(Throwable $e){if($db->inTransaction())$db->rollBack();throw $e;}
