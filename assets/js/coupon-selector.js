(function(){
  const money=value=>`฿${Number(value||0).toLocaleString('th-TH',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
  function initCouponSelectors(scope=document){
  scope.querySelectorAll('[data-coupon-selector]').forEach(root=>{
    if(root.dataset.couponInitialized==='1')return;
    root.dataset.couponInitialized='1';
    const select=root.querySelector('[data-coupon-select]'),input=root.querySelector('[data-coupon-input]'),applyButton=root.querySelector('[data-coupon-apply]'),message=root.querySelector('[data-coupon-message]');
    const total=document.getElementById(root.dataset.totalTarget),discount=document.getElementById(root.dataset.discountTarget),discountRow=document.getElementById(root.dataset.discountRow),shipping=document.getElementById(root.dataset.shippingTarget),tax=document.getElementById(root.dataset.taxTarget);let subtotal=Number(root.dataset.subtotal||0),baseTotal=Number(root.dataset.baseTotal||subtotal),baseShipping=Number(root.dataset.baseShipping||0),baseTax=Number(root.dataset.baseTax||0);
    const syncPayable=value=>document.querySelectorAll('[data-payable-total]').forEach(el=>el.textContent=money(value));
    const showSelected=codes=>{const selected=Array.isArray(codes)?codes.filter(Boolean):[];message.textContent=selected.length?`ใช้คูปอง: ${selected.join(' + ')}`:'คำสั่งซื้อนี้จะไม่ใช้คูปอง';};
    const reset=()=>{if(total)total.textContent=money(baseTotal);syncPayable(baseTotal);if(shipping)shipping.textContent=baseShipping>0?money(baseShipping):'ฟรี';if(tax)tax.textContent=money(baseTax);if(discount)discount.textContent='-฿0.00';if(discountRow)discountRow.style.display='none';};
    const post=async(params)=>{const body=new URLSearchParams({...params,csrf_token:CSRF_TOKEN});const response=await fetch(`${BASE_URL}api/promotions.php`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});return {response,result:await response.json()};};
    const useCode=async code=>{code=String(code||'').trim();if(!code)return clearCoupon();message.textContent='กำลังตรวจสอบคูปอง...';const {response,result}=await post({action:'validate',code});if(response.status===401){message.textContent='เข้าสู่ระบบเพื่อใช้คูปอง';message.style.color='#b83b32';return;}if(result.status!=='success'){message.textContent=result.message||'ใช้คูปองไม่ได้';message.style.color='#b83b32';reset();return;}input.value=result.data.code;select.value=result.data.code;showSelected(result.data.selected_codes);message.style.color='#28704a';if(discount)discount.textContent=`-${money(result.data.discount)}`;if(discountRow)discountRow.style.display=Number(result.data.discount)>0?'flex':'none';if(shipping)shipping.textContent=Number(result.data.shipping_amount)>0?money(result.data.shipping_amount):'ฟรี';if(tax)tax.textContent=money(result.data.tax_amount);if(total)total.textContent=money(result.data.total);syncPayable(result.data.total);};
    const clearCoupon=async()=>{await post({action:'clear'}).catch(()=>{});select.value='';input.value='';message.textContent='คำสั่งซื้อนี้จะไม่ใช้คูปอง';message.style.color='var(--muted)';reset();};
    select.addEventListener('change',()=>select.value?useCode(select.value):clearCoupon());
    applyButton.addEventListener('click',()=>input.value.trim()?useCode(input.value):clearCoupon());
    root.addEventListener('cart:updated',event=>{subtotal=Number(event.detail?.subtotal||0);baseTotal=Number(event.detail?.total||subtotal);baseShipping=Number(event.detail?.shipping||0);baseTax=Number(event.detail?.tax||0);const code=String(select.value||input.value||'').trim();code?useCode(code):reset();});
    fetch(`${BASE_URL}api/promotions.php?action=mine`).then(async response=>({response,result:await response.json()})).then(({response,result})=>{if(response.status===401){select.hidden=true;message.textContent='เข้าสู่ระบบเพื่อเลือกคูปองที่รับไว้';return;}const coupons=result?.data?.coupons||[];coupons.forEach(coupon=>{const option=document.createElement('option');option.value=coupon.code;const benefit=coupon.discount_type==='percent'?`ลด ${Number(coupon.discount_value)}%`:coupon.discount_type==='fixed'?`ลด ${money(coupon.discount_value)}`:'ส่งฟรี';option.textContent=`${coupon.title} · ${benefit} · ${coupon.code}`;select.append(option);});const selected=result?.data?.selected_codes||[];const active=selected.filter(code=>coupons.some(c=>c.code===code));if(active.length){const latest=active[active.length-1];select.value=latest;input.value=latest;useCode(latest);}else reset();}).catch(()=>{message.textContent='โหลดคูปองไม่สำเร็จ';});
  });
  }
  window.initCouponSelectors=initCouponSelectors;
  initCouponSelectors();
})();
